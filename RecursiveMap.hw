'program': "Simple recursive map function Example .Hw"
| (@map [f]
    $ <<= (@[list]
        $ <<= :: <$list == {}
              -- {}
              -- f [<$list] <$> map [f] [<*list] 
    $)
$)
| @main[]
    $ numbers: {1, 2, 3, 4, 5}
    | squareN: (@[n]$ <<= n * n $)
    | echo [map [squareN][numbers]]
$
| main[]